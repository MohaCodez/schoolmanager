import { HighlightNADirective } from './highlight-na.directive';

describe('HighlightNADirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightNADirective();
    expect(directive).toBeTruthy();
  });
});
